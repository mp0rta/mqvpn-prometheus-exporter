# Example Prometheus scrape config for mqvpn-prometheus-exporter.
# Drop into prometheus.yml's scrape_configs: section.
scrape_configs:
  - job_name: mqvpn
    scrape_interval: 15s
    static_configs:
      - targets: ['127.0.0.1:9091']
